{
  "name": "auth",
  "version": "1.0.0",
  "description": "auth service to handle user authentication, registration and login",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pg": "^8.7.1"
  }
}
